[project]
name = "deep-research-agent"
version = "0.1.0"
description = "Multi-agent research assistant xây trên LangGraph"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["agent"]
